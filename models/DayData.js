export default class DayData {
    constructor(date, events, readings, details, title, fastTitle, fastLevel, saints, holidays) {
      this.date = date;
      this.events = events;
      this.readings = readings;
      this.details = details;
      this.title = title;
      this.fastTitle = fastTitle;
      this.fastLevel = fastLevel;
      this.saints = saints;
      this.holidays = holidays;
    }
  }
  