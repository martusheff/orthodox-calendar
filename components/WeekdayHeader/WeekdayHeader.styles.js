import { StyleSheet } from "react-native";

export default StyleSheet.create({
    row: {
        flexDirection: 'row',
    },
    cell: {
        width: '14.28%',
        justifyContent: 'center',
        alignItems: 'center',
        padding: 5,
    },
});
